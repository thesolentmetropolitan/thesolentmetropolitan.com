{#
/**
 * @file
 * Theme override for an Event node in the teaser view mode.
 *
 * Title is not linked because rabbit_hole blocks node access for events;
 * the field_url field is the actionable link instead.
 *
 * Field display order is taken from the teaser view mode configuration:
 * core.entity_view_display.node.event.teaser.yml
 */
#}
{%
  set classes = [
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
    'slnt-event-teaser',
    content.field_image|render|striptags|trim ? 'slnt-event-teaser--has-image' : 'slnt-event-teaser--no-image',
  ]
%}

<article{{ attributes.addClass(classes) }}>

  {% if content.field_image|render|striptags|trim %}
    <div class="slnt-event-teaser__image">
      {{ content.field_image }}
    </div>
  {% endif %}

  <div class="slnt-event-teaser__text">
    <h2 class="slnt-event-teaser__title">{{ label }}</h2>

    {{ content|without('field_image') }}
  </div>

</article>
