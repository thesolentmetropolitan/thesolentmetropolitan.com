/**
 * @file
 * customsolent behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.customsolent = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
