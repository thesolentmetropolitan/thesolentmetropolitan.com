Phase-1 config for scripts/release-2026-09-19.sh — copies of the two
`field_body` config files from config/sync, imported with `--partial`
BEFORE the body data is migrated. Safe to delete once that release is
live. Not part of normal config sync.
